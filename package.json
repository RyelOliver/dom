{
  "name": "dom",
  "version": "1.0.0",
  "description": "An extension to `xmldom`'s JavaScript implementation of W3C DOM. Most notably `getAllNodesByPath` and `getNodeByPath` are to be used in a manner similar to `querySelectorAll` and `querySelector`. Also provides syntactic sugar through a set of chainable functions.",
  "main": "DOM.js",
  "scripts": {
    "test": "jest --watch"
  },
  "keywords": [
    "javascript",
    "dom",
    "xml",
    "xmldom",
    "queryselector",
    "chain",
    "chainable",
    "syntax",
    "synactic",
    "sugar"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^6.0.1",
    "jest": "^24.8.0",
    "xmldom": "^0.1.27"
  }
}